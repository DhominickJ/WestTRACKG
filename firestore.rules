rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /processing/{document=**} {
      allow read, write: if request.auth != null && request.auth.token.sub != null;
    }
  }
}